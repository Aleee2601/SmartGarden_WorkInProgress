<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:SmartGarden.App.ViewModels"
             x:Class="SmartGarden.App.Views.LoginPage"
             x:DataType="viewmodel:LoginViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource Primary}">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <!-- Logo/Title -->
            <Label Text="🌱 SmartGarden"
                   FontSize="48"
                   FontAttributes="Bold"
                   TextColor="White"
                   HorizontalOptions="Center"
                   Margin="0,0,0,40"/>

            <Label Text="Welcome Back"
                   FontSize="24"
                   TextColor="White"
                   HorizontalOptions="Center"
                   Margin="0,0,0,20"/>

            <!-- Login Form -->
            <Border Stroke="{StaticResource Gray200}"
                    StrokeThickness="1"
                    Background="White"
                    Padding="20"
                    StrokeShape="RoundRectangle 10">

                <VerticalStackLayout Spacing="15">

                    <!-- Email -->
                    <Label Text="Email" TextColor="{StaticResource Gray900}"/>
                    <Entry Text="{Binding Email}"
                           Placeholder="your.email@example.com"
                           Keyboard="Email"
                           ReturnType="Next"/>

                    <!-- Password -->
                    <Label Text="Password" TextColor="{StaticResource Gray900}"/>
                    <Entry Text="{Binding Password}"
                           Placeholder="Enter password"
                           IsPassword="True"
                           ReturnType="Done"
                           ReturnCommand="{Binding LoginCommand}"/>

                    <!-- Error Message -->
                    <Label Text="{Binding ErrorMessage}"
                           TextColor="Red"
                           IsVisible="{Binding ErrorMessage, Converter={StaticResource StringIsNotNullOrEmptyConverter}}"
                           FontSize="12"/>

                    <!-- Login Button -->
                    <Button Text="Login"
                            Command="{Binding LoginCommand}"
                            BackgroundColor="{StaticResource Primary}"
                            TextColor="White"
                            HeightRequest="50"
                            Margin="0,10,0,0"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"/>

                    <ActivityIndicator IsRunning="{Binding IsBusy}"
                                     IsVisible="{Binding IsBusy}"
                                     Color="{StaticResource Primary}"
                                     HeightRequest="30"/>

                    <!-- Register Link -->
                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="5">
                        <Label Text="Don't have an account?"
                               TextColor="{StaticResource Gray600}"/>
                        <Label Text="Register"
                               TextColor="{StaticResource Primary}"
                               TextDecorations="Underline">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateToRegisterCommand}"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </HorizontalStackLayout>

                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
